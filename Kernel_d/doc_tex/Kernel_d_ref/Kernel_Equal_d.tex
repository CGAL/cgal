\begin{ccRefFunctionObjectConcept}{Kernel::Equal_d}
A model for this must provide:

\ccCreationVariable{fo}

\ccMemberFunction{bool operator()(const Kernel::Point_d&p, const
  Kernel::Point_d&q);} {returns true iff $p$ and $q$ are equal (as
  $d$-dimensional points). \ccPrecond{\ccc{p} and \ccc{q} have the
  same dimension.}}

\end{ccRefFunctionObjectConcept}
