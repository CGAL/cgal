\begin{ccRefFunction}{side_of_bounded_sphere}
\ccInclude{CGAL/predicates_d.h}

\ccFunction{template <class ForwardIterator> Bounded_side
  side_of_bounded_sphere( ForwardIterator first, ForwardIterator last,
  const Point_d<R>& p);} { returns the relative position of point
  \ccc{p} to the sphere defined by \ccc{A = tuple [first,last)}. The
  order of the points of $A$ does not matter.  \ccPrecond
  \ccc{orientation(first,last)} is not \ccc{ZERO}.  \ccRequire{ The
  value type of \ccc{ForwardIterator} is \ccc{Point_d<R>}.  }}

\end{ccRefFunction}
