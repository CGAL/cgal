\begin{ccRefFunctionObjectConcept}{Kernel::Component_accessor_d}
A model for this must provide:

\ccCreationVariable{fo}

\ccMemberFunction{int dimension(const Kernel::Point_d& p);} {returns
  the dimension of $p$.}

\ccMemberFunction{ Kernel::RT homogeneous(const Kernel::Point_d& p,
  int i);} {returns the ith homogeneous coordinate of $p$.  \ccPrecond{
  \ccc{0 <= i <= dimension(p)}.}}

\ccMemberFunction{ Kernel::FT cartesian(const Kernel::Point_d& p, int
  i);} {returns the ith Cartesian coordinate of $p$.  \ccPrecond{
  \ccc{0 <= i < dimension(p)}.}}

\end{ccRefFunctionObjectConcept}
