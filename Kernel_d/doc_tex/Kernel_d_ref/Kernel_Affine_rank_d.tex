\begin{ccRefFunctionObjectConcept}{Kernel::Affine_rank_d}
A model for this must provide:

\ccCreationVariable{fo}

\ccMemberFunction{template <class ForwardIterator> int
  operator()(ForwardIterator first, ForwardIterator last);}{computes
  the affine rank of the points in \ccc{A = tuple [first,last)}.
  \ccPrecond{The objects are of the same dimension.}
  \ccRequire{ The value type of \ccc{ForwardIterator} is
  \ccc{Kernel::Point_d}.}}

\end{ccRefFunctionObjectConcept}
