This directory contains the following files:

makefile_<os>_<compiler> : 

	Include makefiles with platform dependent settings. These
	files will be generated during installation.

	<os> and <compiler> are short descriptors of the operating
	system and the compiler for which the include file is
	intended. For information on how these descriptors are
	generated, see the installation manual.
