

the steps to generate package's reference manual from the source code:

0. replace template tex files with the ones provided in the archive (Class and FunctionObjectClass),
by default they are installed to $HOME/local/Manual_tools/latex_conv_config/

1. run create_skeleton_doc to generate skeleton documentation files

2. edit generate_reference_manual_Doxyfile (located in the same directory as the original script) 
to ensure the following settings are present:

ENABLE_PREPROCESSING   = YES
MACRO_EXPANSION        = YES
INLINE_INHERITED_MEMB  = YES

3. run generate_reference_manual

generate_reference_manual -I path/to/package/include path/to/package

4. run cgal_manual from /doc_tex/ subdirectory

