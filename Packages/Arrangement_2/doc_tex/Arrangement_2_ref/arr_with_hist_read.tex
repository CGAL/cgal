% +------------------------------------------------------------------------+
% | Reference manual page: arr_with_hist_read.tex
% +------------------------------------------------------------------------+
% | 
% | Package: Arrangement_2
% | 
% +------------------------------------------------------------------------+

\ccRefPageBegin

\begin{ccRefFunction}{read (Arrangement_with_history_2)}

\ccInclude{CGAL/IO/Arr_with_history_iostream.h}

\ccFunction{template<class Traits, class Dcel, class Formatter>
            std::istream& read (Arrangement_with_history_2<Traits,Dcel>& arr,
                                std::istream& is,
                                Formatter& formatter);}
   {Reads the arrangement-with-history instance \ccc{arr} from the given 
    input stream using a specific input format. \ccc{formatter}, which must
    be a model of the \ccc{ArrWithHistoryInputFormatter}, defines the input
    format.}

\ccFunction{template<class Traits, class Dcel>
            std::istream& operator>> (std::ostream& is,
                                      Arrangement_with_history_2<Traits,Dcel>& arr);}
   {Reads the arrangement-with-history instance \ccc{arr} from the given
    input stream using the default input format.}

\end{ccRefFunction}

\ccRefPageEnd
