### Section: Meta data
## Example with zero matrix, that is row rank=0, at beginning of phaseII the
## set of active constraints is empty

with(LinearAlgebra):

desc:="Example with zero matrix, that is row rank=0, at beginning of phaseII the set of active constraints is empty":

my_name:="QP_Degenerate_rank_zero_QPE_solver":

### Section: Data 

A:=Matrix([[0, 0, 0], [0, 0, 0], [0, 0, 0]]):

relop:=Matrix([[0], [0], [0]]):

b:=Matrix([[0],[0], [0]]):

c:=Matrix([[-1, -3, 4]]):

Q:=Matrix([[1, 15, 0], [-15, 1, 0], [0, 0, 1]]):

### Section: Generation of test data file(s)

src_path:="./":

src:=cat(src_path, "encodeProblem"):

dest_path:="../data/":

dest:=cat(dest_path, my_name):

gen_desc:=StringTools['Join'](["Generated by ", my_name, ".mw"], ""):

read src:

encodeQpRatProblem(A, relop, b, c, Q, desc, gen_desc, dest):

encodeQpIntProblem(A, relop, b, c, Q, desc, gen_desc, dest):

encodeQpFloatProblem(A, relop, b, c, Q, desc, gen_desc, dest):
