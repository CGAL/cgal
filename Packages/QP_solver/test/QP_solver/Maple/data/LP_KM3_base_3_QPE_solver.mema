### Section: Meta data
## 3-dim KleeMinty example with base=3

with(LinearAlgebra):

desc:="3-dim KleeMinty example with base=3":

my_name:="LP_KM3_base_3_QPE_solver":

### Section: Data 

A:=Matrix([[1, 0, 0], [6, 1, 0], [18, 6, 1]]):

relop:=Matrix([[-1], [-1], [-1]]):

b:=Matrix([[9], [81], [729]]):

c:=Matrix([[-9, -3, -1]]):

### Section: Generation of test data file(s)

src_path:="./":

src:=cat(src_path, "encodeProblem"):

dest_path:="../data/":

dest:=cat(dest_path, my_name):

gen_desc:=StringTools['Join'](["Generated by ", my_name, ".mw"], ""):

read src:

encodeLpRatProblem(A, relop, b, c, desc, gen_desc, dest):

encodeLpIntProblem(A, relop, b, c, desc, gen_desc, dest):

encodeLpFloatProblem(A, relop, b, c, desc, gen_desc, dest):
