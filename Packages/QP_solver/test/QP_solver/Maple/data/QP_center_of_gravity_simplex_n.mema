### Section: Meta data
## Computes center of gravity of n-dim simplex, creates large bases

with(LinearAlgebra):
is_part_of_CGAL_testsuite:= true:

desc:="Computes center of gravity of n-dim simplex, creates large bases":

my_name:="QP_center_of_gravity_simplex_n":

### Section: Data 

n := 16:

A:=Matrix(1, n, 1):

relop := Matrix([[0]]):

b:=Matrix([[1]]):

c:=Matrix(1,n,-2/n):

c_float:=convert(c, float):

Q:=Matrix(n,n, shape=identity):

### Section: Generation of test data file(s)

src_path:="./":

src:=cat(src_path, "encodeProblem"):

dest_path:="../data/":

dest:=cat(dest_path, my_name):

gen_desc:=StringTools['Join'](["Generated by ", my_name, ".mw"], ""):

read src:

encodeQpRatProblem(A, relop, b, c, Q, desc, gen_desc, dest):

encodeQpFloatProblem(A, relop, b, c_float, Q, desc, gen_desc, dest):
