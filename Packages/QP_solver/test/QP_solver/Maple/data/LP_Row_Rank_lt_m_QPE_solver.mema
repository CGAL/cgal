### Section: Meta data
## Example with row rank less than m for removing an arificial variable and its
## equality constraint

with(LinearAlgebra):

desc:="Example with row rank less than m for removing an arificial variable and its equality constraint":

my_name:="LP_Row_Rank_lt_m_QPE_solver":

### Section: Data 

A:=Matrix([[3, 4, 2, 5, 1], [2, 1, 3, 0, -1], [1, 2, 0, 3, 1]]):

relop:=Matrix(3,1, 0):

b:=Matrix([[6], [-1], [4]]):

c:=Matrix(1, 5, 1):

### Section: Generation of test data file(s)

src_path:="./":

src:=cat(src_path, "encodeProblem"):

dest_path:="../data/":

dest:=cat(dest_path, my_name):

gen_desc:=StringTools['Join'](["Generated by ", my_name, ".mw"], ""):

read src:

encodeLpRatProblem(A, relop, b, c, desc, gen_desc, dest):

encodeLpIntProblem(A, relop, b, c, desc, gen_desc, dest):

encodeLpFloatProblem(A, relop, b, c, desc, gen_desc, dest):
