### Section: Meta data
## Example with zero objective function vector

with(LinearAlgebra):

desc:="Example with zero objective function vector":

my_name:="LP_U6_zero_c_QPE_solver":

### Section: Data 

A:=Matrix([[1, 0/1], [0/1,1/1], [3/4, 1/1], [1/1, 3/4]]):

relop:= Matrix([[-1],[-1], [-1],[-1]]):

b:= Matrix([[1], [1], [3/2], [3/2]]):

c:=Matrix([[0, 0]]):

### Section: Generation of test data file(s)

src_path:="./":

src:=cat(src_path, "encodeProblem"):

dest_path:="../data/":

dest:=cat(dest_path, my_name):

gen_desc:=StringTools['Join'](["Generated by ", my_name, ".mw"], ""):

read src:

encodeLpRatProblem(A, relop, b, c, desc, gen_desc, dest):
