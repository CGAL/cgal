The directory auxiliary (with gmp-4).
It has Cygwin related patch applied.
As well, precompiled libs for MSVC and Borland are packaged here.

It also contains a stripped down version of the boost tarball
(only the include directory).
