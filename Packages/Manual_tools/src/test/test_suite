#!/bin/csh -f
# 
# Test suite for the tools around the cc_manual.sty for C++ manual writing.
# ------------------------------------------------------------------------
# No messages should be visible during the run of the test suite. Any
# message visible indicates an error.

# +---------------------------------------------------------------+
# |   test1                                                       |
# +---------------------------------------------------------------+
# Test the tools on a simple file implementing a single class, 
# CGAL_Point_2 (from a previous CGAL manual).
cd test1
./test_suite
cd ..

# +---------------------------------------------------------------+
# |   test2                                                       |
# +---------------------------------------------------------------+
# Test the tools on many more features. Apply the tools to the
# test.tex file located in ./Tools/format/ . Maybe the copy used
# here won't be the most up to date file.
cd test2
./test_suite
cd ..

# +---------------------------------------------------------------+
# |   test3                                                       |
# +---------------------------------------------------------------+
# This part tests in particular the new reference manual style and
# its conversion to HTML with the example given in ../../example/.
cd test3
./test_suite
cd ..


