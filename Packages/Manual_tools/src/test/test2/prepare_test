#!/bin/csh -f
# +---------------------------------------------------------------+
# |   prepare_test for test2                                      |
# +---------------------------------------------------------------+

# Prepare test suit for the tools around the cc_manual.sty 
# for C++ manual writing.
# ------------------------------------------------------------------------

# Test of the extraction
cc_extract test.tex > test.ori.ex

# Test of the manual production
rm html_ori/*

cc_manual_to_html -release "Release 1.6" -date "Date" -cgal_dir . -o html_ori -show_main test.tex  -aux test.aux -bbl test.bbl

