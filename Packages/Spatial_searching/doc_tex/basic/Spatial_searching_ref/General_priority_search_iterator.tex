% +------------------------------------------------------------------------+
% | Reference manual page: General_priority_search::iterator.tex
% +------------------------------------------------------------------------+
% | 1.07.2001   Johan W.H. Tangelder
% | Package: ASPAS
% |
\RCSdef{\RCSGeneralprioritysearch::iteratorRev}{$Revision$}
\RCSdefDate{\RCSGeneralprioritysearch::iteratorDate}{$Date$}
% |
%%RefPage: end of header, begin of main body
% +------------------------------------------------------------------------+


\begin{ccRefClass}{General_priority_search<Traits, QueryItem, Distance>::iterator}  %% add template arg's if necessary

%% \ccHtmlCrossLink{}     %% add further rules for cross referencing links
%% \ccHtmlIndexC[class]{} %% add further index entries

\ccDefinition

The class \ccRefName\ implements an input iterator
for the computation of approximate nearest and furthest neighbours.

\ccInclude{CGAL/Priority_search.h}

\ccParameters

Expects for the parameter \ccc{Traits} an implementation of the concept TreeTraits.
Expects for the parameter \ccc{QueryItem}  of a $d$-dimensional point, for example \ccc{CGAL::Point_d}
or an implementation of a spatial object, for
example \ccc{Iso_rectangle_d} implementing iso-rectangles.
Expects for the parameter \ccc{Distance} an implementation of the
concept GeneralDistance.

\ccCreation
\ccCreationVariable{i}  %% choose variable name

\ccConstructor{iterator();}{Default constructor. Constructs an past-the-end iterator.}
\ccConstructor{iterator(iterator& Iter);}{Copy constructor.}
\ccConstructor{iterator(Tree& tree, QueryItem& q, Traits tr, Distance& d, int k, NT Eps,
bool Search_nearest=true);}
{Creates an iterator for searching approximately neighbours of the query item \ccc{q}
in the items stored in the $k$-$d$ tree \ccc{tree}.}

\ccHeading{Member Functions}

\ccMethod{Item_with_distance* operator*();}
{Returns a pointer to the pair consisting of the next neighbour and its distance
to the query item.}

\ccMethod{
iterator& operator++();}
{Prefix operator.}

\ccMethod{
std::auto_ptr<Item_with_distance> iterator& operator++(int);}
{Postfix operator.}

\ccMethod{
bool operator==(const iterator& It);}
{The equality operator.}

\ccMethod{
bool operator!=(const iterator& It);}
{The inequality operator.}

{\bf Non-Member functions related to \ccc{General_priority_search<Traits,QueryItem,Distance>::iterator}}

\ccGlueBegin
\ccGlobalFunction{ template <class Traits, QueryItem, Distance>
void swap(General_priority_search<Traits, QueryItem, Distance>::iterator& x,
General_priority_search<Traits, QueryItem, Distance>::iterator& y);}
{Swaps the contents of \ccc{x} and \ccc{y}.}
\ccGlueEnd

\ccSeeAlso

\ccc{General_priority_search}.


\end{ccRefClass}

% +------------------------------------------------------------------------+
%%RefPage: end of main body, begin of footer
% EOF
% +------------------------------------------------------------------------+

