README
======

next revision:
- check if linda override is needed for #369997
	/usr/share/linda/overrides/libcgal-dev:
	# override bogus warning caused by #369997
	Tag: exec-not-elf-or-script
	Data: /usr/bin/cgal_create_makefile

backport for sarge:
- debian/control:
  * adjust Standards-Version: to 3.6.1
  * adjust debhelper dependency from >= 5 to >= 4
- debian/compat:
  * adjust debhelper compatibility level from 5 to 4
- debian/changelog:
  * adjust version suffix from -X to -0sargeY
  * adjust suite from unstable to stable
  * maintain other differences
