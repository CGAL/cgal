\begin{ccRefFunctionObjectConcept}{Kernel::ConstructTranslatedPoint_2}
A model for this must provide:

\ccCreationVariable{fo}

\ccMemberFunction{Kernel::Point_2 operator()(const Kernel::Point_2& p,
                                             const Kernel::Vector_2& v);}
       {returns the point obtained by translating \ccc{p} by the vector 
        \ccc{v}.}

\ccMemberFunction{Kernel::Point_2 operator()(const Origin& o,
                                             const Kernel::Vector_2& v);}
       {returns the point obtained by translating a point at the origin by the vector 
        \ccc{v}.}

\ccRefines
\ccc{AdaptableFunctor} (with two arguments)

\ccSeeAlso
\ccRefIdfierPage{CGAL::Point_2<Kernel>}\\

\end{ccRefFunctionObjectConcept}
