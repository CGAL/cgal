\begin{ccRefFunctionObjectConcept}{Kernel::ConstructPoint_2}
A model for this must provide:

\ccCreationVariable{fo}

\ccMemberFunction{Kernel::Point_2 operator()(const Origin &ORIGIN);}
            {introduces a variable  with Cartesian coordinates
              $(0,0)$.}

\ccHidden\ccMemberFunction{Kernel::Point_2 operator()(int x, int y);}
            {introduces a point  initialized to $(x,y)$.}

\ccHidden\ccMemberFunction{Kernel::Point_2 operator()(double x, double y);}
            {introduces a point  initialized to $(x,y)$.}

\ccHidden\ccMemberFunction{Kernel::Point_2 operator()(const Kernel::RT &hx, const Kernel::RT &hy, const Kernel::RT &hw = Kernel::RT(1));}
            {introduces a point  initialized to $(hx/hw,hy/hw)$.
             If the third argument is not explicitly given, it defaults
             to \ccStyle{Kernel::RT(1)}.
             \ccPrecond{\ccc{hw} $\neq$ \ccc{Kernel::RT(0)}.} }

\ccHidden\ccMemberFunction{Kernel::Point_2 operator()(const Kernel::FT &x, const Kernel::FT &y);}
            {introduces a point  initialized to $(x,y)$.}

\ccRefines
\ccc{AdaptableFunctor} (with one argument)

\ccSeeAlso
\ccRefIdfierPage{CGAL::Point_2<Kernel>} \\

\end{ccRefFunctionObjectConcept}
