TODO:
----
- Make sure the documentation will make it into the main tree...
- operator/(cl_I, cl_I) is not provided.
- Factorize the common code of these files in a separate file ?
  (is_valid, etc... ?)
- Things that are supported by Gmpz, and should (?) be ported to cl_I:
  - exact_division  (exquo())
  - approximate_decimal_length
  - convert_to<IA>
- Make benchmarks.
- Test-suite: un fichier générique, testé successivement pour tous les NTs ?
- Aller voir les snapshots sur le site FTP...  Attendre la prochaine version
  et s'adapter (il y a min/max/abs, les include *io.h, etc...).
- Il y a un portage sous CygWin.
