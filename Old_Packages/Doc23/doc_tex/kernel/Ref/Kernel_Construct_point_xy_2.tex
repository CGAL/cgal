\begin{ccRefFunctionObjectConcept}{Kernel::Construct_point_xy_2}
A model for this must provide:

\ccCreationVariable{fo}


\ccMemberFunction{Kernel::Point_2 operator()(const Kernel::Point_3 &p);}
            {introduces a variable with Cartesian coordinates $(x_p, y_p)$, 
             which is the projection of point \ccc{p} onto the $xy$-plane.}

\end{ccRefFunctionObjectConcept}
