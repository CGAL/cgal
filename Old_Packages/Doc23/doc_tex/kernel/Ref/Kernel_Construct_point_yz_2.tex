\begin{ccRefFunctionObjectConcept}{Kernel::Construct_point_yz_2}
A model for this must provide:

\ccCreationVariable{fo}


\ccMemberFunction{Kernel::Point_2 operator()(const Kernel::Point_3 &p);}
            {introduces a variable with Cartesian coordinates $(y_p, z_p)$, 
             which is the projection of point \ccc{p} onto the $yz$-plane.}

\end{ccRefFunctionObjectConcept}
