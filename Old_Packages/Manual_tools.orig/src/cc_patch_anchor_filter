#!/usr/local/bin/perl -pi

# Patch the links in the anchor translator to achieve crosslinks between 
# the different directories.
s/(<A HREF=[\\][\"])([^\/\"]*[\\][\"]>)/$1$ENV{CC__LOC_F}$2/g;
s/(wrap_anchor[(] [\"])/$1$ENV{CC__LOC_F}/g;
