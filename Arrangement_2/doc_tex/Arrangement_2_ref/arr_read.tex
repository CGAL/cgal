\ccRefPageBegin

\begin{ccRefFunction}{read}

\ccDefinition

%%%%
  
Reads the arrangement \ccc{arr} from the given input stream using a
specific input format. \ccc{formatter}, which must be a model of the
\ccc{ArrangementInputFormatter}, defines the input format.
   
\ccInclude{CGAL/IO/Arr_iostream.h}

\ccGlobalFunction{template<class Traits, class Dcel, class Formatter>
            std::istream& read (Arrangement_2<Traits,Dcel>& arr,
                                std::istream& is,
                                Formatter& formatter);}

%%%%

Reads the arrangement-with-history instance \ccc{arr} from the given 
input stream using a specific input format. \ccc{formatter}, which must
be a model of the \ccc{ArrWithHistoryInputFormatter}, defines the input
format.

\ccInclude{CGAL/IO/Arr_with_history_iostream.h}

\ccGlobalFunction{template<class Traits, class Dcel, class Formatter>
            std::istream& read (Arrangement_with_history_2<Traits,Dcel>& arr,
                                std::istream& is,
                                Formatter& formatter);}

\ccSeeAlso
  \ccc{write}\lcTex{(\ccRefPage{CGAL::write})}

\end{ccRefFunction}

\ccRefPageEnd
