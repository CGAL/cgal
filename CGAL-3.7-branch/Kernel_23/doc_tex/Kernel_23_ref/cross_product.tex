\begin{ccRefFunction}{cross_product}

\ccFunction{Vector_3<Kernel> cross_product( const Vector_3<Kernel>& u, 
                                       const Vector_3<Kernel>& v);}
       {returns the cross product of $u$ and $v$.}
\end{ccRefFunction}

